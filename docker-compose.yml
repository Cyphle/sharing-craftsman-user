version: '3'

services:
  user:
    build: .
    container_name: sc_user
    ports:
      - <APP_PORT>
    volumes:
     - <LOGS_PATH>:/logs

networks:
  default:
    external:
      name: <DOCKER_NETWORK>